# OpenAPI & API Reference Documentation

This directory contains generated OpenAPI specification artifacts (`openapi.json`).

Hand-editing files in this folder is forbidden. Specs are automatically emitted by FastAPI on application startup or build step.
